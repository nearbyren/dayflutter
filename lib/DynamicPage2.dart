import 'package:flutter/material.dart';

class DynamicPage2 extends StatelessWidget {
  const DynamicPage2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DynamicPage2'),
      ),
    );
  }
}

