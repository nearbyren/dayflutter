import 'package:flutter/material.dart';

class DynamicPage3 extends StatelessWidget {
  const DynamicPage3({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DynamicPage3'),
      ),
    );
  }
}

