import 'package:flutter/material.dart';

class DynamicPage4 extends StatelessWidget {
  const DynamicPage4({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('DynamicPage4'),
      ),
    );
  }
}

